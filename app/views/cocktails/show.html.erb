
<h1><%= @cocktail.name %></h1>


<div class="card">
  <h4>Dose Names</h4>
  <ol>
  <% @doses.each do |dose| %>
    <li>
      <p><%= dose.description %> with <%= dose.ingredient.name %><%= link_to "🗑️", dose_path(dose), method: :delete %></p>
    </li>
  <% end %>
  </ol>

</div>


<p><%= link_to "Go back", root_path  %></p>

<p><%= link_to "Add new ingredient", new_cocktail_dose_path(@cocktail)  %></p>

